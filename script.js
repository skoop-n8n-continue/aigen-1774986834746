// Simple script to handle any generic initialization or cache-busting logic if needed
document.addEventListener('DOMContentLoaded', () => {
    console.log('Hello World Digital Signage App initialized.');

    // As per requirements, digital signage should be non-interactive
    // and run automatically. We use CSS animations for the visual entry.
});